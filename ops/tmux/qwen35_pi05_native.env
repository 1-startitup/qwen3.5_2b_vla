#!/usr/bin/env bash

# Core paths
ROOT="${ROOT:-/home/frankkkz/qwen35_2b_vla}"
PY_ENV="${PY_ENV:-/home/frankkkz/miniconda3/envs/lerobot_pi0}"
PYTHON_BIN="${PYTHON_BIN:-$PY_ENV/bin/python}"
ACCELERATE_BIN="${ACCELERATE_BIN:-$PY_ENV/bin/accelerate}"

# Session metadata
SESSION_NAME="${SESSION_NAME:-qwen35-pi05}"
RUN_NAME="${RUN_NAME:-qwen_3.5_2b_pi0.5_v2_lora_r16_bs32_ga2_30k_native}"
CHECKPOINT_STEP="${CHECKPOINT_STEP:-30000}"
PROJECT_CONTEXT_DOC="${PROJECT_CONTEXT_DOC:-$ROOT/ops/tmux/PROJECT_CONTEXT.md}"
PROJECT_CONTEXT_JSON="${PROJECT_CONTEXT_JSON:-$ROOT/ops/tmux/project_context.json}"

# Training configuration
TRAIN_CONFIG="${TRAIN_CONFIG:-$ROOT/config/libero_train_qwen_3_5_2b_pi0_5_v2_lora_r16_30k_native.yaml}"
ACCEL_CONFIG="${ACCEL_CONFIG:-$ROOT/config/accelerate_single_gpu_bf16.yaml}"
TRAIN_LOG="${TRAIN_LOG:-$ROOT/train_v2_bs32_ga2_30k_native.log}"
TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-32}"
TRAIN_GRAD_ACCUM_STEPS="${TRAIN_GRAD_ACCUM_STEPS:-2}"
TRAIN_MAX_STEPS="${TRAIN_MAX_STEPS:-30000}"
TRAIN_WANDB_RUN_NAME="${TRAIN_WANDB_RUN_NAME:-$RUN_NAME}"
TRAIN_OUTPUT_DIR="${TRAIN_OUTPUT_DIR:-$ROOT/checkpoints/$RUN_NAME}"

# Evaluation targets
CHECKPOINT_DIR="${CHECKPOINT_DIR:-$ROOT/checkpoints/$RUN_NAME/checkpoint-$CHECKPOINT_STEP}"
OPEN_LOOP_JSON="${OPEN_LOOP_JSON:-}"
OPEN_LOOP_LOG="${OPEN_LOOP_LOG:-}"
FIXED_STANDARD_JSON="${FIXED_STANDARD_JSON:-}"
FIXED_STANDARD_LOG="${FIXED_STANDARD_LOG:-}"
FIXED_VIDEO_JSON="${FIXED_VIDEO_JSON:-}"
FIXED_VIDEO_LOG="${FIXED_VIDEO_LOG:-}"
FIXED_VIDEO_ROLLOUT_DIR="${FIXED_VIDEO_ROLLOUT_DIR:-}"

# Desktop bundles
DESKTOP_VIDEO_DIR="${DESKTOP_VIDEO_DIR:-}"
DESKTOP_ANALYSIS_DIR="${DESKTOP_ANALYSIS_DIR:-}"

# Runtime defaults
CUDA_VISIBLE_DEVICES="${CUDA_VISIBLE_DEVICES:-0}"
NUM_INFERENCE_STEPS="${NUM_INFERENCE_STEPS:-10}"
CHUNK_SIZE="${CHUNK_SIZE:-50}"
DETERMINISTIC_SEED="${DETERMINISTIC_SEED:-0}"
VIDEO_EPISODES="${VIDEO_EPISODES:-1}"
VIDEO_RECORD_TASKS="${VIDEO_RECORD_TASKS:-10}"
VIDEO_RECORD_EPISODES="${VIDEO_RECORD_EPISODES:-1}"
OPEN_LOOP_BATCH_SIZE="${OPEN_LOOP_BATCH_SIZE:-16}"
OPEN_LOOP_WORKERS="${OPEN_LOOP_WORKERS:-2}"
OPEN_LOOP_MAX_BATCHES="${OPEN_LOOP_MAX_BATCHES:-50}"

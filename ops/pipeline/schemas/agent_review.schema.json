{
  "type": "object",
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["accept", "rollback", "ambiguous"]
    },
    "dominant_bucket": {
      "type": "string",
      "enum": [
        "A_refresh_or_gripper",
        "B_continuity",
        "C_grounding_or_subtask",
        "mixed"
      ]
    },
    "evidence": {
      "type": "array",
      "items": {"type": "string"},
      "minItems": 1
    },
    "next_version": {
      "type": "string"
    },
    "notes": {
      "type": "array",
      "items": {"type": "string"}
    }
  },
  "required": ["decision", "dominant_bucket", "evidence", "next_version", "notes"],
  "additionalProperties": false
}
